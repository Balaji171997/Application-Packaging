---
name: gpf-testcases-remediation
description: "GPF team test-case findings (Package_BuilderTesting.docx) + fixes; decisions, gold standard, per-increment plan"
metadata: 
  node_type: memory
  type: project
  originSessionId: f4fc59ce-bb2a-4ffe-b080-0f251cb9e612
  modified: 2026-07-30T04:46:56.611Z
---

GPF team returned test results in `C:\Users\AW140\Downloads\testcases\Package_BuilderTesting.docx` (35 screenshots) + 5 AES request folders each with a manually-authored **predecessor** package (the ground truth). Work is in `Downloads\GPF-PackageBuilder` ONLY. See [[gpf-brand-variant]].

**Gold standard = Freia 9.1.0** `...\AES-1-020658-A VW_Freia...\Predecessor\VW_Freia_x64_9.1.0-0001_MUL\Content\Invoke-AppDeployToolkit.ps1` — a hand-authored v4 GPF package. Correct shapes: `AppScriptDate='2026-04-20'` (yyyy-MM-dd), author no comma, `AppProcessesToClose=@()` empty, `VWG_FreeSpaceUninst='200'`, `VWG_SoftinstTyp='Legacy'` for exe, `VWG_SoftIdent='HKLM:\SOFTWARE\VWG\CM\<FullName>'` for Legacy (MSI uses Uninstall\{GUID}), `VWG_Portfv='VW'` (=appVendor), branding+`#Set-Reboot` LAST in Post-*, `$flag=$false` in CUSTOM VARIABLES, uninstall sets `$flag=$true`, Post-Uninstall `If($flag){ Remove-Branding }`. GPF v4 uses `##==== MARK ====` banners ONLY (no `#*====` ones).

**User decisions (asked & answered):**
1. Banners: use OUR template's banners; strip the predecessor's; NEVER duplicate. (Template currently has BOTH `##====` MARK and `#*====` styles → the `#*====` are the duplicates to drop; but they're injection anchors — see remaining.)
2. MST: surface BOTH the MSI **Property table** AND **CheckBox table** (Orca-like) combined with the predecessor's MST changes, editable, then create.
3. Predecessor reuse: manual selection is fine; when reusing, **swap the predecessor app-name → current app-name EVERYWHERE** (SoftIdent, logs, uninstall block, etc.).

**Two build paths** (Build.ps1): `Build-FreshScript` (~1228, snapshot/no-pred) and `Build-PredecessorScript` (~1366, reuse). Both end at `Format-OutputScript`. Template: `lib\PSADT_Template_GPF\Content\Invoke-AppDeployToolkit.ps1`. Placeholders `{Manufacturer}/{Softwarename}/{Version}/{Typ}/{Date}/{Author}` are filled via `Set-SessionField`/`Set-SessionValue` (NOT `Set-TemplatePlaceholders`, which handles the different `{ScriptDate}/{ScriptAuthor}` set). `Extract-SessionValues` (Predecessor.ps1:224) reads predecessor VWG_ fields.

**DONE — Increment 1 (all Test-Build asserts green):**
- Predecessor.ps1: extractor regex now matches `$Global:VWG_`/`$Script:` prefix AND double-bracket `[string[]]` type (was missing both → Freia's SoftinstTyp/Portfv/SoftIdent never read). Empty proc list → `@()` not `''`.
- Build.ps1: new `Format-AuthorName` (strip comma), `Set-WrapperIfBlank`, `Set-GpfWrapperDefaults` (fills SoftinstTyp {Typ}→Legacy/MSI, Portfv→vendor, OrderNumber→Ritm, FreeSpace→300 floor, Legacy SoftIdent→CM key; only when blank/placeholder). Called at end of BOTH build paths.
- Date `MM/dd/yyyy`→`yyyy-MM-dd` (Build-Fresh 1236, Build-Pred 1540, Set-TemplatePlaceholders 604). Author comma-stripped in both paths.
- `Set-SectionBody`: Post-* injects body BEFORE the `## Branding`/`## Handling for required reboot` trailer (branding/reboot stay LAST).
- `Format-OutputScript`: catch-all collapse `\Uninstall\ {GUID}` → `\Uninstall\{GUID}`.

**DONE — Increment 2 (v3→v4 mappings, Test-Build green):** verified vs bundled `PSAppDeployToolkit.psm1` v4.1.5. Execute-MSI Params += `-AddParameters`→`-AdditionalArgumentList`, `-Parameters`→`-ArgumentList`, `-SecureParameters`→`-SecureArgumentList`, `-Patch`→`-Patches` (PSADT_V3toV4_Mappings.ps1:32). `Set-ServiceStartMode` Params `{-Name→-Service, -StartupType→-StartMode}` (:114, v4 param is `-Service` ServiceController). `-IfEmpty` 4a converter already works (case OK); added a Format-OutputScript safety net that strips a leftover `Remove-ADTFolder -IfEmpty` from preserved verbatim blocks. `Copy-ADTFile` Test-Path wrapper still TODO.

**DONE — Increment 3 (real-time generate+diff verification, Test-Build green):** Built a harness `scratchpad\gen-diff.ps1` (Read-PredecessorModel -PackageName + Build-PredecessorScript, GPF settings.json auto-gives GPF brand) — generate from a testcase predecessor, diff line-by-line vs the hand-authored gold standard. Confirmed Increment-1 fills all reflect. Fixes found & landed:
  - **Issue A** (extra code): template's generic scaffold log lines (`"...$appVendor $appName $appVersion..."`) DUPLICATED the predecessor's authored logs in Main-Install/Uninstall/Repair. Set-SectionBody now drops them when the body owns its action logging.
  - **Issue B** (unwanted structure): empty `If($flag){}` + orphan `$flag=$true` left in the uninstall-previous block (branding-removal boilerplate stripped). Get-UninstallBody now removes the empty guard + orphan flag ($flag belongs to the CURRENT package's Uninstall fn).
  - **Banner de-dup** (decision 1): Format-OutputScript (GPF-gated) strips all `#*====PHASE BEGIN/END====` lines → output is MARK-fence-only, matching gold standard exactly (incl. CUSTOM APPLICATION VARIABLES region). Add-PsadtRunKeyRemovals made marker-agnostic (Get-PBMarkerSet) so the disk run-key injector still finds POST-INSTALL.
  - New-UninstallPreviousBlock: `deployAppScriptFriendlyName`→`DeployAppScriptFriendlyName` (capital, matches gold).
  - **Two-place SoftIdent VERIFIED working** (synthetic x86 two-SoftIdent predecessor): wrapper (plain, no token) + CUSTOM VARIABLES (double-quoted, `$($VWG_CurrentRegWOW)` token kept) BOTH survive with version swapped. No code change needed — the GPF gate at Build.ps1:~1533 already keeps the custom-var one. NOTE: none of the 5 testcases actually have the two-place pattern (all single wrapper SoftIdent, no token) — it's an x86 convention (Qbit-DB style).

**DONE — flag-logic branding (Test-Build green):** `Set-GpfFlagGuardedBranding` (Build.ps1) — when the Uninstall fn sets `$flag=$true`, wraps the Post-Uninstall `## Branding Uninstall / Remove-Branding` in `If($flag){}` and drops the dead empty guard. **VERIFIED: generated Freia 9.1.1 now matches the hand-authored gold standard line-by-line across ALL sections** (Variables, Pre-Install+uninstall-prev, Install, Post-Install, Uninstall, Post-Uninstall, Repair, Invocation).

**DONE — Predecessor name-swap (decision 3, Test-Build green, real-gen verified):** Build.ps1 §3b (right after Invoke-VersionSwap, line ~1628, BEFORE uninstall-prev blocks injected/shielded): `$out = $out.Replace($Model.Identity.AppName, $NewPkg.AppName)` when they differ. VERIFIED Tenable RISLAPAC→AS: current package refs (InstallTitle, install/uninstall/repair logs) → NessusAgentAS; old-version uninstall blocks (10.4.0.20152, 10.4.2.20158) correctly KEEP RISLAPAC (they remove the actually-installed old packages). Harness gen-diff.ps1 gained -NewAppName.

HTML placement, WOW6432Node tokenize, and Copy-ADTFile Test-Path guard are all DONE (Test-Build green, real-gen verified). See the "DONE — HTML placement + WOW6432Node + Copy-ADTFile" note above.

**REMAINING increments (mapped):**
- **Folder structure** (Zscaler image16): SupportFiles under `Content\Files\SupportFiles`, `.mst` under `Content\Files` — ASSEMBLY-side (Assemble.ps1).
- **Complexity matrix** not copied to package Documents; **MRF name** = current package name — assembly-side.
- **MST viewer** (decision 2): show MSI Property + CheckBox tables + predecessor MST diff, editable — GUI feature (largest remaining).
- Custom functions not v4-converted (Zscaler); predecessor-of-predecessor double-handling in pre-install.
- Snapshot install failing on their RDP boxes (env, not tool) — degrade gracefully (defaults now cover empty fields).
- AUDI request name doesn't parse (`AUDI AG Audi Workbench 4.0 Rev4`) — process note: inform AUDI EQS; maybe lenient parse.
- OBSERVED (deferred, assess w/ user): Tenable pre-install has "##Uninstalling <current> if present" (current-version self-uninstall) — doc: "Current version is being handled in pre-installation, predecessor doesn't."

**DONE — Complexity matrix exclusion:** BrandGPF.ps1 doc collection now skips `*complexity*` files (GPF-only; MTB still harvests them per Test-Build:449).

**PACKED r2:** BuildStamp `GPF-2026-07-16.r2`; `Pack-Engine.ps1` parse-gate passed (428 KB pak); portable `Downloads\GPF_PackageBuilder\PackageBuilder.pak` refreshed (with .bak). All Test-Build green.

**Verify harness:** `scratchpad\gen-diff.ps1 -PredPath <predfolder> -NewVersion <v> [-NewAppName <name>]` — generates + writes `_generated.ps1`/`_predecessor.ps1` for line-by-line diffing. Test-Build assertions #1–14 lock all the above.

**DONE — folder/MRF/MST viewer (all Test-Build green; render-tested; PACKED r3):**
- Folder layout (user: follow v4 standard, don't copy a wrongly-kept predecessor): `Invoke-SupportFilesHoist` (Assemble.ps1) hoists any `Content\Files\SupportFiles` OUT to `Content\SupportFiles`; converter folds `$($adtSession.ScriptDirectory)\SupportFiles`→`$($adtSession.DirSupportFiles)` (and \Files→DirFiles).
- MRF rename (user: `<origbase>_<FullName>.xlsx`, idempotent case-fix): `Get-MrfTargetName` helper + rename pass in New-Package; strips a prior `_<vendor>_...` suffix, `-ceq` skip when already exact.
- **MST viewer (decision 2)**: `Get-MsiCheckBoxTable` (MstBuilder.ps1); `MsiPropRow` now `INotifyPropertyChanged` (+`MsiCheckRow`); `Show-MsiPropertiesDialog` rewritten — editable Property grid + read-only CheckBox-table grid + **amber change-highlight** RowStyle DataTrigger on `Changed` (readable light-on-dark) + predecessor pre-fill (`-PredecessorProps`) + legend + Note column. `$script:State.PredMstProps` set in `BtnMatchPredMst` from `$s.ExtraProps`; passed to the single-MSI caller. Render-tested offscreen (INotify Changed toggles live; WPF graph builds; reader graceful on bad path).

**BuildStamp `GPF-2026-07-16.r3`**, pak 432 KB, portable `Downloads\GPF_PackageBuilder` refreshed (.bak kept). Test-Build assertions #1–17 all green.

**DONE — custom functions carried (Test-Build green, real-gen verified, PACKED r4):** ROOT CAUSE = no `CustomFunctions` section marker, so the predecessor's CUSTOM APPLICATION FUNCTIONS body (Zscaler: Show-HTMLInstallationWelcome + Show-HTMLWelcomePrompt + Update-InstallationWelcome, ~800 lines) was DROPPED while the call survived → broken script (GPF v4 toolkit does NOT provide Show-HTMLInstallationWelcome). Fix: added `CustomFunctions` marker to BOTH `$script:SectionMarkers` and `$script:SectionMarkersGpfV4` (Predecessor.ps1), and excluded it from Strip-Boilerplate (carry verbatim; whole-content Convert-V3ToV4Content already made the bodies v4). Verified: 3 funcs carried, Show-HTMLInstallationWelcome now DEFINED, 0 v3 residue, parse OK (728→1514 lines). Empty CUSTOM FUNCTIONS = no-op (Set-SectionBody leaves the section pristine).

**BuildStamp `GPF-2026-07-16.r4`**, pak 432 KB, portable refreshed (.bak). Test-Build #1–18 green (now 11 section markers).

**DONE — Round 2 changes (Tenable round reviewed line-by-line; Test-Build #1-21 green; PACKED r5):**
- **A (env/zip customs)**: GPF keeps `Set-EnvironmentVariable`/`Expand-ZipFile`/`Remove-EnvironmentVariable` (their Extensions module defines them) — added to `$script:MtbOnlyFunctions` (PSADT_V3toV4_Mappings.ps1). Also added `Set-ADTEnvironmentVariable` param map `-EnvironmentVariable→-Variable, -EnvironmentValue→-Value` (for MTB's MtbMappings-on case; verified vs main psm1 line 15665).
- **FreeSpaceUninst FORCED to 200** (Set-GpfWrapperDefaults, Set-SessionValue override — was carrying predecessor's 500).
- **D (pred-of-pred removal)**: GPF drops `$preservedOldBlocks` (only the immediate-predecessor generated block remains). Build.ps1 ~1690, GPF-gated. Verified Tenable: 10.4.0.20152 block GONE, 10.4.2.20158 kept.
- **E (branding in immediate block)**: New-UninstallPreviousBlock adds `Remove-Branding -InstanceName "<predFullName>" -AdditionalRegPaths "...$($VWG_CurrentRegWow)VWG\InstalledProducts","...CM"` before any trailing Set-Reboot, GPF-gated. Verified Tenable.
- **C (reboot preserved)**: GPF ALREADY correct — Strip-Boilerplate line 183 strips only COMMENTED `#Set-Reboot` (active kept); Remove-PreRepairNoise only touches MTB `Set-MTBReboot`; Remove-GpfRebootPlaceholder strips the template placeholder when predecessor has active reboot. Verified Tenable immediate block keeps `Set-Reboot`.

**MTB (Downloads\files) — DONE per user "do A,B,C,D on MTB; D immediate-only for both" (MTB Test-Build green, PACKED r199, local portable Downloads\PackageBuilder refreshed):**
- A: Set-EnvironmentVariable→Set-ADTEnvironmentVariable with `-EnvironmentVariable→-Variable, -EnvironmentValue→-Value` (PSADT_V3toV4_Mappings.ps1:132). B: added `Expand-ZipFile→Expand-MTBZipFile` mapping (MTB Extensions defines Expand-MTBZipFile).
- C: Strip-Boilerplate (Predecessor.ps1:139) + Remove-PreRepairNoise (Build.ps1:233) now strip ONLY commented `#Set-MTBReboot` (`#*`→`#+`) — an ACTIVE predecessor Set-MTBReboot is kept.
- D: dropped `$preservedOldBlocks` injection (Build.ps1:~1480) UNCONDITIONALLY (MTB immediate-only too — removed the Synera two-block stacking). REWROTE the 6 obsolete MTB tests (TWO blocks→ONE, older-preserved→dropped, opt-out→zero, nCode pred-of-pred CHECKED/UNCHECKED→2023.1 dropped). New MTB asserts lock A/B/C.
- `Get-MrfTargetName`/MST-viewer etc. are GPF-only, not ported to MTB.

**DEPLOYED + FINAL VERIFICATION (user-authorised):**
- MTB r199 uploaded to the team share `\\mndemucfsm01\SEC-EQS-Lib-Gate\EQS_SEC\EQS\Script Repository\VWITS Team\Balaji\PackageBuilder\PackageBuilder.pak` (old pak backed up to `.bak-20260717-054541`; Hidden+ReadOnly re-applied). Users auto-pick-up via self-stage Copy-IfNewer. (GPF has no live share; its local portable Downloads\GPF_PackageBuilder is the distribution.)
- Line-to-line verification harness `scratchpad\verify-all.ps1` generates ALL 5 GPF testcase packages (Freia/Zscaler/Tenable+swap/GlobalProtect/Gandalf-zip via ZipFile.ExtractToDirectory to C:\temp\gpv for MAX_PATH) and runs 12 checks each — ALL PASS, braceDelta=0 (count LCurly+AtCurly vs RCurly; `@{` = one AtCurly token), 0 parse errs, 0 v3 residue. Generated scripts saved as scratchpad\_gen_*.ps1.

**DONE — Round 3 (4 user issues, GPF; Test-Build #1-23 green, 5-pkg re-verify clean, PACKED r6):**
1. **Review false-positive**: `Get-ScriptReviewFindings` gained `-NewProductCode`; skips the "SoftIdent PC carried from predecessor" warning when the SoftIdent GUID == the new package's PC (correctly swapped). Callers: Get-PredecessorReport (NewPkg.ProductCode) + GUI review refresh (State.ProductCode).
2. **Current-version uninstall in pre-install**: `Remove-SelfUninstallGuard` (Build.ps1) strips the predecessor's bare "##Uninstalling '<...>' if present" block (header + uninstall + folder/TAG/IfEmpty cleanup, brace-tracked) from PreInstallCode — it's redundant with the generated immediate-predecessor block. Wired after Split-ExistingUninstallBlocks. Tenable 611→594 lines, others unchanged.
3. **Predecessor documents leaking**: BrandGPF.ps1 doc collection now excludes `[\\/]predecessor[\\/]` in the install-instruction scan AND a final `$r.DocItems` guard drops any item under a Predecessor folder (GUI already filtered State.Resolved.DocItems at ~2648, but $req.DocItems was merged unfiltered).
4. **MST viewer**: `MsiPropRow` now has `OrigValue`/`InMsi` + a COMPUTED `Note` = "NEW property" / "changed (was 'X')" / else the checkbox/predecessor `ExtraNote`. Added an "Original (MSI)" read-only dimmed column so value changes read old→new side-by-side. BuildStamp r6.
- Round-3b (conservative stripper): `Remove-SelfUninstallGuard` rewritten to consume ONLY the guard's own known statements and STOP at anything else — proven that custom pre-install code before AND after the guard survives (no missing pre-install code). r7.

**DONE — Round 4 (2 user asks, GPF; Test-Build #24 added, all green; PACKED r8):**
1. **"Match predecessor MST" button REMOVED**: converted its handler to `function Set-PredecessorMstAuto` (GUI.ps1) — no confirmation dialog; auto-applies the predecessor MST as the DEFAULT (PredMstProps + shortcut/Run-key Keep toggles + replicated can-apply removals + review notes) when the predecessor loads. Called from Set-PredecessorUi + the Step-2 refresh, guarded by a `PredMstAppliedKey` (predecessor|MSI) so it applies ONCE per combo and never clobbers user edits. Button now always Collapsed. LblMatchMst repurposed as an auto-apply status line. (Show-MstPlanDialog now dead code, left harmless.)
2. **Multiple predecessors for GPF**: was offering only the latest — `Resolve-GpfRequest` now exposes `PredecessorRoot` (the whole Predecessor\ folder) and `Get-GpfPredecessorCandidates` enumerates ALL its subfolders (+ the zip-extracted path), newest-first. Verified real GlobalProtect request → 4 candidates (6.2.8-0002/6.2.8-0001/6.2.5/6.2.4). BuildStamp r8.

AUDI name parse — user handles (inform AUDI EQS). Deferred: Tenable current-version-self-uninstall in pre-install.

**DONE — Round 5 (fresh-package quality batch, 6 user asks; GPF-only, MTB untouched; Test-Build 530/0, MTB 478/0; PACKED r23):**
1. **Predecessor from fetched source** (GUI.ps1 BtnPred): when GpfRequest isn't set, FIRST climb from the chosen installer's dir via `Find-GpfRequestRoot` (predecessor in the `Predecessor\` folder NEXT TO the source wins), THEN fall back to the settings `RepositoryPath` (`Find-GpfRequestFolder`).
2. **Fresh SoftIdent — ARCH-GATED** (Build.ps1 Build-FreshScript, GPF/hardWow=false): wrapper ALWAYS = PLAIN single-quoted no-token (`Format-GpfSoftIdentPlain`). The `$($VWG_CurrentRegWOW)` two-place re-assignment in CUSTOM APPLICATION VARIABLES (`Add-GpfCustomVar`) is **x86-ONLY** (GandalfClient style — WoW6432Node only redirects on 32-bit). **x64 = single plain wrapper, NO token** (Freia x64 style); emitting the token on x64 is wrong syntax (user finding, Tenave NessusAgentAS x64). Gate: `if ("$($NewPkg.Arch)" -match '(?i)x86|32')`. MTB branch unchanged.
3. **Single product → ProductCode only** (Snapshot.ps1 `Get-UninstallFromSnapshotDiff`): after collecting AllUninstalls, collapse entries grouped by normalised DisplayName+DisplayVersion — a group with a ProductCode entry drops its EXE-uninstaller duplicate (msiexec /x wins). Suite components (distinct names) untouched.
4. **Service disable/remove USER CHOICE** (Snapshot.ps1 `Get-SnapshotCleanups`): each added service now emits TWO exclusion rows — "Disable service" (`Stop-ADTServiceAndDependencies` + `Set-ADTServiceStartMode -StartMode Disabled`, Get-Service-guarded) and "Remove service" (team extension `Delete-Service -Name`, stops+sc.exe delete → avoids "marked for deletion"). NOT raw Stop-Service/Set-Service. Post-install/post-uninstall selector already per-row.
5. **Log dedup / command placement** (Build.ps1 `Set-GpfMainSuccessLog`, GPF-gated): upgrades the Main-section bare `"<Action> of $appVendor $appName $appVersion."` scaffold log → "... is successful." so Set-SectionBody's success-anchor drops the install command BETWEEN the Start/success logs (gold layout) instead of after both. (CRLF trap: no `$` anchor — .NET `$` matches before `\n`, `\r` sits before it.)
6. **MRF brand prefix** (Assemble.ps1): `Get-MrfTargetName` regex allows optional brand prefix before vendor; MRF rename includes `OutPrefix` (INA_/VWG_/G1V_). Idempotent (no `_INA_INA_`).

Test-Build var-collision bug fixed en route: `$aTpl` fixture == `$atpl` (PS names case-insensitive) clobbered the real template → renamed to `$scaffTpl`.

**BuildStamp `GPF-2026-07-20.r23`**, pak 451/461 KB (parse-gate 0 errs; decrypt+parse verified; x64 SoftIdent = single plain no-token confirmed from the portable pak).

**DONE — Round 6 / finding #48 CLOSED (user decision): never hardcode WoW6432Node for GPF — keep the `$VWG_CurrentRegWOW` variable — and define SoftIdent TWICE (main WITHOUT the token, custom-variables WITH it), in BOTH the fresh AND reuse paths.**
- New shared `Set-GpfSoftIdentTwoPlace -Text -Arch` (Build.ps1, HARD brand guard: returns unchanged unless brand=GPF, because `Set-SessionValue -Field SoftIdent` would otherwise rewrite MTB's bare `SoftIdent =` line and strip its hardcoded WoW segment). Takes the effective value from the custom-vars copy (it wins at runtime) else the wrapper; sets wrapper = `Format-GpfSoftIdentPlain`; adds/refreshes the tokened custom-vars copy **only when x86 AND the value is a `...\Uninstall\...` detection key** (a `VWG\CM\` branding key stays single+plain, Freia style). Idempotent; expression-based hand-crafted detections don't match the simple-literal regex so they're left alone.
- Called at the END of BOTH `Build-FreshScript` and `Build-PredecessorScript` (after `Set-GpfWrapperDefaults`, which may fill the CM key).
- **Latent bug fixed** in `Merge-SnapshotDeltas` (~line 1409): on snapshot-refresh it wrote `Format-BrandSoftIdent` (the TOKENED double-quoted form) via `Set-SessionValue`, i.e. into the FIRST occurrence = the WRAPPER. Now GPF writes the PLAIN form to the wrapper and the tokened copy to custom-vars (x86 only).
- Reuse with REAL predecessors verified: Gandalf x86 keeps two-place with version swapped 6.1.0→6.2.0; Freia x64 single plain CM key; 0 hardcoded WoW6432Node in either.
- Test-Build 540/0 (7 new `GPF 2place:` asserts incl. idempotency + the CM-key and x64 negative cases). MTB 478/0 unaffected.

**Testcases docx cross-check (all 80 lines read via OOXML extract):** every actionable finding across Tenable/Zscaler/GlobalProtect/Freia/Gandalf is implemented. Non-code leftovers: #78 Gandalf "installation gives UI prompt" = vendor silent switches, not a tool defect; #6/44/56/73 snapshot install failing = their RDP environment (user believes the MSI run-spec fix covers it); #79/80 AUDI incoming format = user informs AUDI EQS.

**TWO GPF FOLDERS — deploy gotcha (caused a "pak not updated" report):** `Downloads\Application-Packaging\GPF-PackageAssistance` (HYPHEN) = the SOURCE dir (all .ps1, Pack-Engine writes the pak HERE). `Downloads\Application-Packaging\GPF_PackageAssistance` (UNDERSCORE) = the PORTABLE the user actually RUNS (exe + pak + Lib + settings.json, NO source). Pack-Engine does NOT copy between them. After every pack you MUST also copy the fresh pak from hyphen→underscore, else the user runs a stale pak. And a running exe caches the pak in memory — restart the app to pick up a new pak. (Folders + artifacts were `GPF-PackageBuilder`/`GPF_PackageBuilder` + `PackageBuilder.exe/.pak` before the 2026-07-24 rename below.)

**DONE — GPF full rebrand to "Package Assistance" incl. binary/folder names (2026-07-24, r32):** window title/dialogs/Intune-notes/PBShots-comment → "Package Assistance"; `Get-PBToolName` (Core.ps1) DEFAULTS to "Package Assistance" in the GPF build (no dependence on settings.json), Brand.ToolName still overrides; added ToolName to source+portable settings.json. **Renamed artifacts**: `PackageAssistance.exe` (recompiled Loader.ps1 via ps2exe — 54KB GUI PE, old kept as `PackageBuilder.exe.old`), `PackageAssistance.pak` (Loader+Pack-Engine reference the new name; AES key unchanged), `PackageAssistance.exe.config`. **Renamed folders**: source `GPF-PackageAssistance`, portable `GPF_PackageAssistance`. Cleaned up all the accumulated `*.pak.bak-*` clutter. Verified: portable pak decrypts+parses clean (= what the loader does at launch). MTB stays "Package Builder" (its exe/pak/folder unchanged). See [[downloads-files-is-pb-only]] for the live path list.

**DONE — Remove-BrandingREG → Remove-Branding conversion + Set-EnvironmentVariable clarification (2026-07-24, r33; GNS_Animator4 testing):**
- **Set-EnvironmentVariable is NOT a bug for GPF** — the GPF Extensions module (`PSAppDeployToolkit.Extensions.psm1`) DEFINES+EXPORTS `Set-EnvironmentVariable`/`Remove-EnvironmentVariable`, so it's a valid GPF v4 fn kept verbatim (it's in `$script:MtbOnlyFunctions`, skipped for GPF at PSADT_V3toV4_Mappings.ps1:449). MTB has no such extension → MTB converts it to standard `Set-ADTEnvironmentVariable -Variable/-Value` (mapping :121). So per-brand-different-and-correct.
- **Remove-BrandingREG → Remove-Branding (NEW, GPF-only):** both are exported GPF fns, but `Remove-Branding` is the modern TOP-LEVEL one (removes the WMI `VWG_SoftwareBranding` instance AND calls Remove-BrandingREG internally for the registry); `Remove-BrandingREG` is registry-ONLY. Added a GPF-gated block in Convert-V3ToV4Content (LAYER 1c-GPF, right after the MTB -AdditionalRegPaths strip): drops `-BrandingKey "..."` (Remove-Branding has no such param, uses house default HKLM:\SOFTWARE\VWG\CM internally), renames the call, KEEPS -Name + -AdditionalRegPaths (incl. the `$($VWG_CurrentRegWow)` token, since flagWow off for GPF). Gated `-not $flagMtb` so MTB's Remove-MTBDetectionKey model is untouched. Asserts: NF branding / NF env in GPF Test-Build.

**DONE — doc-harvest: dependency-folder exclusion + alt install-instructions name (2026-07-24, r34; GPF, BrandGpf.ps1 Resolve-GpfRequest):**
- **Never collect documents from a "dependency" folder** (like the existing Predecessor exclusion — a dependency folder's docs belong to a bundled dependency app, not this package). Shared `$excludeDoc = '(?i)([\\/]predecessor[\\/]|[\\/][^\\/]*dependenc)'` used in BOTH the recursive install-instruction search filter AND the final DocItems guard. `dependenc` (with the 'c') matches dependency/dependencies anywhere in a path SEGMENT (App_Dependencies, 3rd Party Dependency) but NOT "independent". Scoped to DOC harvesting only — the installer PAYLOAD copy is untouched (dependencies may be needed to install).
- **Alternative install-instructions doc name**: the team may NAME the instructions doc "Software Package Request Form", so added `software.?package.?request` to the install-instruction BaseName regex (alongside install.*instru / installation.*guide / how.?to.?install). Asserts: NF docs in GPF Test-Build.

**DONE — predecessor-zip MAX_PATH extraction failure (2026-07-24, r35; SAP_EnableNowProducer 295MB zip):** error "Cannot find path ...\PredCache\<zipname>\<zipname>\Content\ does not exist". ROOT CAUSE: the zip wraps a top folder named after the 46-char package, and the cache dest was ALSO named after the zip → extracted paths hit ~270 (>260 MAX_PATH) → Expand-Archive failed leaving an EMPTY cache folder → the `if (-not Test-Path $cache)` guard then REUSED the empty folder forever. Fixes (Core.ps1 + Predecessor.ps1 Expand-PredecessorZip + BrandGpf.ps1 Expand-GpfPredecessorZip & Get-GpfPredecessorContainer): (1) `Get-PredZipCache` = PredCache base + 8-char SHA1 hash of the zip path (NOT the long name) → every downstream path stays <260 (real zip now maxes 229); (2) `Test-PredZipComplete` = cache has a deployment script → re-extract when the folder exists but is empty/incomplete; (3) `Expand-PBArchive` uses `[IO.Compression.ZipFile]::ExtractToDirectory` (canonical - handles PS5.1 Compress-Archive's trailing-slash-less dir entries; `ExtractToFile`+`\\?\` does NOT work and mis-classifies those) with a `\\?\` FileStream fallback only for >260. Cleared the stale empty caches. Verified: 295MB zip extracts in ~2.6s, 37 items, v3 model loads (AppName=EnableNowProducer). NB the predecessor is v3 (Content\Deploy-Application.ps1, not Invoke-*). Asserts: NF zipfix (+ existing pred-src S1-S4) in GPF Test-Build.

**DONE — SoftIdent DisplayVersion always on real Uninstall keys + zip-source freeze/verbatim (2026-07-24, r36):**
- **SoftIdent DisplayVersion** (user: keep/add `[DisplayVersion=<ver>]` even when the predecessor only had a bare ProductCode, UNLESS it's a VWG\CM branding key): new `Add-SoftIdentDisplayVersion` (Build.ps1) - adds the NEW package version to any `…\Uninstall\…` value lacking it, skips branding keys + already-present + non-Uninstall paths. Wired into `Set-GpfSoftIdentTwoPlace -Version` (both build paths pass `$NewPkg.Version`), so wrapper + x86 custom-vars tokened copy both get it. Asserts: NF SoftIdent.
- **Zip-source (Siemens TmxDIKAB: Sources\Files\Files.zip = 5 GB)**: the fetch used to `Expand-SourceZips` the whole 5 GB (froze the UI for minutes) and shipped the EXTRACTED content. Root of "detected as loose files / copied extracted / not responding". Fix (Source.ps1 Resolve-Source, GPF-gated): NEVER extract a source zip for GPF - keep it as a single VERBATIM loose-zip payload (`ZipPayload` in the result), so New-PayloadZip copies Files.zip as-is (F25 name-preserve) and Get-LooseFilesCommandSet emits `Expand-ZipFile Files.zip -Destination "$envTemp\<App>_<Version>"` for INSTALL-time extraction. Build-Step3Script single-installer switch gained a `.zip` case -> LooseFiles mode. New `Get-ZipInstallerEntries` reads the zip's central directory (INSTANT, ~0.3s for 12847 entries) and returns installer entries shallowest-first (real top-level entry points before deep app binaries). Verified on the real 5 GB source: resolve now ~15s (was minutes), no extraction, index instant. Asserts: NF zipsrc.
- **DONE — zip-source installer PICKER (r37):** `Get-ZipInstallerEntries` now returns the ONE-STEP-in entries (immediate children of the single wrapper folder; incl .bat/.cmd/.ps1, real msi/exe first) - single-pass over the central dir (~0.7s for 12847 entries; normalises `\`↔`/` since .NET CreateFromDirectory writes `\`, real vendor zips write `/`). Set-ResolvedSource (GUI): on `$res.ZipPayload`, reads the inner installers, AUTO-selects a single msi/exe (or a single entry), else `Show-InstallerPicker` (reused). ChosenInstallers become zip-entry pseudo-objects (`InZip`, `RelPath`). State: `ZipPayload`, `ZipInstallers` (+ reset). Build-Step3Script intercepts BEFORE the generic branches -> InstallerMode='ZipPayload' (+ `ZipRunItems`, FreeSpace from zip size ×3). New `Get-ZipPayloadCommandSet` (Build.ps1) + New-StandardCommands case: Expand-ZipFile Files.zip -> `$envTemp\<App>_<Ver>`, then run each selected installer by type (.msi=Start-ADTMsiProcess; .exe=Start-ADTProcess +/S +REVIEW switches; .bat/.cmd=cmd /c; .ps1=powershell -File). Assemble: ZipPayload copies the source .zip VERBATIM into Files\<name> (no extract/re-zip). SupportFiles left untouched (user rule). Asserts: NF zipsrc/zippay. **STILL PENDING**: true async fetch so the ~15s resolve never shows "not responding" (needs a background runspace - GUI threading, deferred). NB the packager still fills EXE silent switches + uninstall (surfaced as REVIEW).

**DONE — invalid-chars + temp/log paths (2026-07-24, r38):**
- **Invalid characters in generated-from-predecessor scripts**: `Format-OutputScript` (Build.ps1, right after the BOM strip, before Invoke-Formatter) now normalises invisible chars a predecessor carries (Word-pasted comments/strings): NBSP variants `\u00A0\u2007\u202F` -> space; zero-width/direction/inline-BOM `\u200B\u200C\u200D\u200E\u200F\u2060\uFEFF` -> removed. (Read-FileSmart + the UTF8+BOM WriteAllText were already correct - the chars came from the source content, not encoding.) Asserts: NF chars.
- **Temp/log paths**: everything now under `C:\temp\GPF-PackageAssistance` (was split: log under `C:\temp\PackageBuilder`, rest under `C:\temp\GPF-PackageBuilder`). Core.ps1 Get-WorkRoot default + the settings-defaults + BOTH settings.json (source+portable) WorkRoot/LocalWorkingDir -> GPF-PackageAssistance; log file renamed `PackageAssistance.log`; `$script:LogFilePath` is now RESET on Load-Settings (was cached to the pre-settings default -> that's why the log leaked to C:\temp\PackageBuilder). GUI fallbacks updated too.

**DONE — duplicate Main log on predecessor reuse + illegal-chars-in-path (r39/r40):**
- **Illegal characters in path** (r39): the zip-installer pseudo-object `FullName` I added used `<zip> + '|' + relpath`; `|` is an illegal path char -> `[IO.Path]`/Get-MsiProductCode/Find-VendorMst threw "Illegal characters in path" the moment the EDITOR page built the script. Fixed: FullName = a syntactically valid `<zip>\<relpath>` (GUI.ps1 Set-ResolvedSource). (Separate from the r38 invisible-char fix.)
- **Duplicate "Start Installation" + bare "Installation of" log** (r40, iDEX Volkswagen_iDEX reuse): `Set-GpfMainSuccessLog` (upgrades the template's bare Main `"<Action> of $appVendor $appName $appVersion."` -> "... is successful.") was ONLY called on the FRESH path (Build.ps1:1797). On PREDECESSOR reuse the bare log survived -> Set-SectionBody's success-anchor (looks for "...successful") missed -> the carried command landed elsewhere and the two template logs stayed consecutive (looked duplicated). Fix: call `Set-GpfMainSuccessLog` on the template at the START of Build-PredecessorScript's inject loop too (Build.ps1 ~1976, GPF-gated). Verified: command now sits between Start and "...is successful", no bare-duplicate. Asserts: NF log.

**INSTALLER-SWAP logic (answered, NOT changed):** predecessor reuse swaps by taking `$Model.Installer.MsiFileName/ExeFileName/ProductCode` (from the predecessor SCRIPT) -> `$NewPkg.*` (from the source the USER SELECTED). Multiple installers pair BY ORDER. Then a VERSION-STRING swap (Invoke-VersionSwap: old-version digits -> new anywhere) + the version-swapped variant of each swap key. So it worked for Siemens because the files differ only by version (version swap caught them) - NOT a "scan all predecessor file refs + fuzzy-match to every current source file (incl. unselected/in-zip)" pass. That comprehensive scan is a possible future feature (risk: wrong matches) - user was told; not implemented.

After each increment: parse-gate + Test-Build, then pack + refresh GPF portable (`Downloads\GPF-PackageBuilder`).

**DONE — Round 7 / New_Findings.docx (2026-07-24; GPF only, MTB `files\` untouched; Test-Build 517/0; PACKED r29; portable refreshed).** Doc at `Downloads\SecondtestCases\New_Findings.docx`; tool folders now under `Downloads\Application-Packaging\` (see [[downloads-files-is-pb-only]]). All fixes verified via `scratchpad\verify_*.ps1` + ungated `NF …` asserts appended to Test-Build (the OLD GPF-template test block SKIPs — its gate `Downloads\OtherBrand` is gone). Per-finding:
- **F2** rename→"Package Assistance": new `Get-PBToolName` (Core.ps1) reads `Brand.ToolName` (added to settings.json), used in GUI window title + captions; XAML fallback/Loader/Build-Exe hardcode it (GPF copy). MTB default stays "Package Builder".
- **F9** predecessor uninstall DisplayName from SoftIdent: `Get-PredecessorDisplayName` (Build.ps1) — strips `_is1`, `{GUID}`→'' (use ProductCode). New-UninstallPreviousBlock EXE case uses it, falls back to parsed AppName (+review item, below).
- **F12** `-Source` → `$adtSession.DeployAppScriptFriendlyName` (added `deployAppScriptFriendlyName` to `$V3ToV4VariablesGpf`).
- **F15** GPF empty-folder = team single-`-and` `Test-Path -PathType Container` + `(Get-ChildItem|Measure).Count -eq 0` + Remove-ADTFolder (converter 4a + safety net); MTB keeps Vithal nested.
- **F18** registry cmdlets `-LiteralPath`→`-Key` (Get-PBHkcuLines).
- **F22** Portfv FORCED to vendor; AppAddInfo01-04→'NA' (Set-GpfWrapperDefaults; `Set-WrapperIfBlank` treats `'blank'` as placeholder).
- **F32** GPF LooseFiles ARP = `Set-/Remove-ApplicationWizardEntry` (no MTB prefix); GUI checkbox label too.
- **F36** MSI-GUID variable `-FilePath`→`-ProductCode` (LAYER 1b-VAR in Convert; guids collected from `$x = '{GUID}'` lines, only rewrites Start-ADTMsiProcess -FilePath $thatvar; real file vars untouched).
- **F40** carried custom-vars `$VWG_SoftIdent` FORCED `$Global` (regex in Set-GpfSoftIdentTwoPlace).
- **F47** SoftIdent DisplayVersion ALWAYS present: fresh MSI = Get-AutoSoftIdent (ProductCode+`[DisplayVersion=ver]`, snapshot-independent); EXE w/ snapshot = real GUID; EXE w/o snapshot = best-guess `…\Uninstall\<AppName> [DisplayVersion=<ver>]` (Set-GpfWrapperDefaults line ~936, replaced the old `VWG\CM\<full>` path — that slipped past review) + review item.
- **F27/F29** MST-generation TOGGLE: GUI `ChkGenerateMst` (default ON) + `$State.GenerateMst`; hides MST option panels when off. Build-Step3Script sets `$newPkg.GenerateMst/MstFileName/NoMst`; `Get-MsiCommandSet -NoMst` (Build.ps1) emits plain msiexec; OFF+source MST (Find-VendorMst)→reuse its name; New-Package `-GenerateMst` gates the assembly MST-build loop.
- **F45** zip predecessor DETECTED: shared `Expand-PredecessorZip` (Predecessor.ps1); Read-PredecessorModel extracts a `.zip` path OR a folder containing a package `.zip` before giving up; BtnPred manual-browse now offers a **zip-file** pick (YesNoCancel). **Real bug fixed** in Expand-PredecessorZip AND Expand-GpfPredecessorZip: a `Content\` at the zip root matched the bare-script test → returned `…\Content` not its parent; now checks cache-root first + excludes `Content`-named folders.
- **F52** progress bar carried: `Set-PredecessorProgressBar` (Build.ps1) uncomments the template's `#Show-ADTInstallationProgress` per section (MAIN-* markers) when the predecessor had it ACTIVE there; called in the reuse merge; +review item. (CRLF `$`-anchor trap again — dropped the `$`.)
- **F54** empty ProcToBlock (already mirrored by Set-ProcToBlockDefault) → review item.
- **F57-59** predecessor SupportFiles helper (sc-uninstall.ps1 etc.) → review item suggesting copy (regex over `$dirSupportFiles`/`DirSupportFiles`/`SupportFiles\<name.ext>`, reuse only).
- **F25/F34** zipped loose source: GPF LooseFiles cmd uses `Expand-ZipFile` (no MTB, same `-Path/-Destination/-Override` sig — GPF Extensions.psm1:2328) extracting to `$envTemp\<AppName>_<Version>` (=C:\Windows\Temp\… under SYSTEM); assembly preserves the SOURCE zip's own name (`$NewPkg.ZipName`, GPF single-zip) instead of `<PkgName>.zip`. MTB keeps Program Files + Expand-MTBZipFile.
- **Review-items audit** (user stressed "softident + appname-as-normal-name were missed"): added 1b (name-based predecessor detection: bare-AppName fallback = strong CHECK-AND-FILL, carried DisplayName = softer confirm; ProductCode detection raises none), fresh bare-app-name SoftIdent now says CHECK AND FILL, 1c/1d/1e = F52/F54/F57 above.
- Deferred per user: F61 keep, F63/64 (user will test), F66 ignore, D-points ignore. Multi-predecessor stays single.
NOTE: F9/F45/F52/F54/F57 review-item logic sits in SHARED functions — currently GPF-only edits (this copy); can be ported to MTB `files\` on request.

**DONE — Intune execution-policy block (2026-07-24; BOTH GPF r30 + MTB r216 packed + portables refreshed).** User hit "running scripts is disabled on this machine / failed to import MSAL module" on Intune — same root cause as the old SCCM console-module fix, but Intune's `Connect-Intune` never lifted the policy. MSAL.PS + IntuneWin32App are SCRIPT modules → `Import-Module` dies when the (32-bit WOW6432Node) process policy is Restricted/Undefined. Fix: new shared `Enable-PBProcessScripts` (Core.ps1) = `Set-ExecutionPolicy -Scope Process Bypass -Force` (in-memory, no admin, idempotent, GPO can still override→warn+continue); called in `Connect-Intune` BEFORE the module import, and SCCM's inline lift refactored to the same helper so no integration forgets it. Intune import catch now hints when the message is execution-policy/GPO. Applied to BOTH codebases (integration modules, not templates). Asserts: NF Intune/SCCM in GPF Test-Build. **DEPLOYED (user-authorised): MTB r216 pushed to the team share** `\\mndemucfsm01\SEC-EQS-Lib-Gate\EQS_SEC\EQS\Script Repository\VWITS Team\Balaji\PackageBuilder\PackageBuilder.pak` (old r215 backed up `.bak-20260725-171456`; temp-then-rename; Hidden+ReadOnly re-applied; verified from share = stamp r216 + guard before MSAL import). Root cause of the persistent failure: the machine launches the SHARE exe/pak and self-stages modules to `%LOCALAPPDATA%\PackageBuilder`, so it ran the OLD share pak — the local `Downloads` portable update alone was never picked up. GPF has no share (local portable only) + Intune feature off, so its fix is future-proofing.

**DONE — Intune create-app HTTP 400 "unable to read JSON request payload" (2026-07-24; MTB r217 to share+portable, GPF r31 portable).** After the exec-policy fix, VS Professional 2026 (6GB) got past MSAL import + .intunewin build, then the FIRST Graph POST (`mobileApps` create, Intune.ps1 line ~735) failed 400. NOT auth: it's a 400 (payload) not 401, the create POST is metadata-only (package size irrelevant to it — the 6GB upload is a LATER step), and every `Invoke-Graph` call already refreshes the token via `Get-IntuneAuthHeader` (auto-refresh within 5 min of expiry). Fix in `Invoke-Graph`: send the body as explicit **UTF-8 bytes** (`[Text.Encoding]::UTF8.GetBytes($json)`, ContentType `application/json; charset=utf-8`, `ConvertTo-Json -Depth 20 -Compress`) instead of a string — PS 5.1 IRM can form-url-encode/re-encode a string body, and bytes make it byte-exact (also stops non-ASCII ®/™ in publisher/description being mangled to `?`). CAVEAT proven locally: ASCII-encoding ®→`?` is still VALID json, so encoding alone may not be the 400's root cause; added a **diagnostic** that logs ContentType+BodyBytes+snippet on any `sc==400 && detail~payload|Content-Type`, so a repeat pinpoints it. Both copies.
