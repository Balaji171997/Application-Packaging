---
name: gpf-30jul-findings-and-brand-selector
description: "GPF 30th-July findings batch (r43/r44) + the Audi/VW/Group brand selector: what's done, decisions taken, what's still open"
metadata: 
  node_type: memory
  type: project
  originSessionId: f4fc59ce-bb2a-4ffe-b080-0f251cb9e612
  modified: 2026-08-04T09:28:39.118Z
---

Source of findings: `C:\Users\AW140\Downloads\SecondtestCases\Finding_For_30th_July.docx` (16 findings + screenshots). GPF codebase only (`Downloads\Application-Packaging\GPF-PackageAssistance` source → `GPF_PackageAssistance` portable). Deployed r43 then r44 (571 Test-Build pass/0).

**Brand selector (user decisions):** DROPDOWN on Step 1 (`CmbBrand`, items Audi=INA / VW=G1V / Group=VWG), default **VWG**, re-applies rules on change via `Invalidate-From 3`. Stored in `State.TargetBrand` → threaded into `$newPkg.TargetBrand`. Engine resolves via `BrandGpf.ps1 Get-GpfTargetBrand` (explicit wins, else `^(INA|VWG|G1V)_` prefix, else VWG). XAML load-verified. **NEEDS in-app verification** (WPF can't be auto-tested).

**DONE + deployed (all Test-Build-covered):**
- #11 output location → `C:\Temp\Working` (settings + Core default, source + portable)
- #4 `Test-Path` no longer double-guards a Copy already inside an outer `If(Test-Path <same src>)`; #5 guard now also covers `Copy-ADTFolder` (Build.ps1 Format-OutputScript Copy guard)
- #6 reuse InstallTitle carries the predecessor's trailing arch suffix e.g. `(x86)` onto the new identity
- #12 GPF name validation in GUI `Parse-Current`: block special chars, ALLOW spaces, reject a space adjacent to `_`
- #8 `Set-GpfFlagGuardedBranding` now takes `-Model` and only adds the `If($flag)` branding guard when the PREDECESSOR itself guarded it (match-predecessor) — keeps branding/reboot ORDER (Move-GpfRebootLast) untouched. Decision: "match predecessor but don't remove the order we decided."
- #13 `Get-GpfInstallTitle`: VW (G1V) shows vendor "Volkswagen" in the title only; Vendor==AppName not repeated (all brands, fresh only)
- #14 `Get-GpfVwgNameLength` + build Warning when brand=VWG and Mfr+Product+Version+Lang > 34 chars; **r53** flagged it at Step 1 (amber warning); **r54 (2026-08-04)** made it a HARD STOP + added an AES-number hard stop. Step-1 gates (GUI.ps1): (a) name format - already blocked via `Parse-Current` return at the Next gate (special chars block, SPACES allowed); (b) Group/VWG 34-char over-limit now sets `$p.IsValid=$false` (red) so Next/step-jump are blocked until shortened; (c) AES number format - `Test-OrderNumberGate` (only when brand OrderNumberLabel='AES') requires `Test-AesNumberFormat` (BrandGpf.ps1, `^AES-\d+-\d+(?:-[A-Za-z0-9]+)?$`, e.g. AES-1-020436-A; empty rejected). Gates wired into BOTH the Next button (Step==1) AND the clickable step-rail forward jump (`N1..N4` MouseLeftButtonDown blocks leaving Step 1 forward if name/AES invalid; backward jumps allowed). CmbBrand SelectionChanged re-runs Parse-Current. Test-Build 599/0 (+7 AES-format asserts near FJ#14)
- #16 `Set-GpfProcToCloseNonUI`: brand=INA (Audi) moves `$Global:VWG_ProcToClose` value into `VWG_ProcToCloseNonUI`, blanks ProcToClose (fresh + reuse)
- #15 `$($VWG_CurrentRegWOW)` x86 SoftIdent — was ALREADY handled (`Set-GpfSoftIdentTwoPlace`); #7 install/uninstall/repair order — already correct (positive)

**#10 DONE (r45):** custom functions from the v4 Extensions module all pass through the converter unchanged. Root cause was `Add-UGPermission` being rewritten to `Set-ADTItemPermission` (with a broken "unusual shape" WARNING) even though the v4 Extensions module DEFINES `Add-UGPermission` natively. Fix: removed that conversion block in `PSADT_V3toV4_Mappings.ps1` (~line 715) so `Add-UGPermission` is preserved. Verified all 58 custom fns preserved + `Remove-BrandingREG`→`Remove-Branding` still renames; 0 mangled. NB: the FJ Test-Build block needs `Brand.Convert.MtbMappings=$false` or branding fns get MTB-mapped.

**r48 (2026-08-03) — indentation part 2:** tab→space (r46) wasn't enough when the command + "Start" log sit at column 0 but only the "…successful" log is pushed right. Added `Build.ps1 Align-GpfScaffoldLogs` (run in Format-OutputScript after Convert-LeadingTabsToSpaces): re-indents ONLY the template scaffold log lines ("Start <Action>…" / "<Action> of … [is successful]." with -Source `$adtSession.DeployAppScriptFriendlyName`) to the nearest install COMMAND's indent (Start-ADTMsiProcess/Process, within 5 lines). SAFE: never touches the command, predecessor/custom code, non-template logs (different -Source), or here-string interiors; output still parses. Verified on the real SAP reuse (command + successful-log both land at the same indent). Test-Build 589/0.

**r46 (2026-08-03) — 3 follow-ups:** (Point1 indentation) `Build.ps1 Convert-LeadingTabsToSpaces` converts LEADING tabs→4 spaces on every line (here-string interiors preserved), run always in Format-OutputScript — because PSScriptAnalyzer/Invoke-Formatter is NOT shipped in the pack, so on a packager machine the formatter is skipped and a stray TAB rendered further right (repro: install cmd at 7 spaces, "is successful" log at TAB+3; TAB→4sp makes it 7 = aligned). (Point3) retired the separate outgoing-prefix dropdown (`CmbOutPrefix`/`PnlOutPrefix` kept collapsed) — `$newPkg.OutPrefix` now = `$script:State.TargetBrand` (Step-1 selection drives the INA_/VWG_/G1V_ folder prefix). (Point2a) NEW **heads-up notices channel**: `Add-GpfNotice`/`Reset-GpfNotices`/`Get-GpfNotices` (`$script:GpfNotices`); GUI resets in `Build-Step3Script`, shows a MessageBox popup ("Heads-up - please read") once per distinct set in `Populate-Step3`. First notice: VW brand selected + REUSE → "Volkswagen install-title rule NOT applied (predecessor reuse keeps its title)". Test-Build 581/0.

**Point2b (review-UX migration) DONE (r47):** 2-tier system. `Build.ps1 Test-GpfCriticalReview` classifies a review line as MUST-SEE (## REVIEW: fill-ins, missing install/uninstall command, name-based detection that may not match ARP, architecture mismatch, source TYPE/STRUCTURE/MULTI-COMPONENT mismatch, CORRUPT) vs nice-to-know (snapshot-added cleanups, MST notes, style hints stay in the list only). `Add-CriticalNoticesFromScript` scans the built script (Get-ReviewItems + Get-ScriptReviewFindings) and PROMOTES the critical ones via `Add-GpfNotice`; called in `Build-Step3Script` after the build. So the heads-up popup now carries the few must-see items + brand notices; the full `LblReview` list still shows everything. Test-Build 587/0.

**#1/#2 DONE + deployed (r49, 2026-08-03, Test-Build 592/0):** `Build.ps1 Set-SectionBody` PRE-INSTALL branch generalized — ANY `$Pre=$true` section body is now inserted at the `$htmlAnchor` (the user-dialogs/Show-*InstallationWelcome block) BEFORE that block, not appended at end. So predecessor custom PRE-INSTALL code (e.g. a Dot-Net dependency check) lands AFTER the reboot-check and BEFORE the process-close/dialogs block, exactly as in the predecessor. #2: new `Add-GpfBodyIndent` re-indents the carried body to the anchor's leading indent (`$anchorIndent`/`$firstLead`/`$inBody`) so it's not left at column 0; here-string-safe. Permanent asserts FJ#1 (position) + FJ#2 (re-indent, `@()`-guarded line pick).

**#1/#2 BUGFIX (r55, 2026-08-04) - only ABOVE-dialogs code goes above; BELOW-dialogs code stays below.** r49 dumped the WHOLE predecessor PRE-INSTALL body before the dialogs. Root cause: `Strip-Boilerplate` (Predecessor.ps1) removes the user-dialogs + reboot blocks and CONCATENATES the above/below custom code, losing position. Fix: Strip-Boilerplate now emits a `#__PB_DIALOGS_SPLIT__` sentinel where the dialogs block was; `Set-SectionBody` (Build.ps1) splits the body on it - ABOVE part lands between the reboot check and dialogs, BELOW part lands after the dialogs block (GPF PRE section ends with dialogs), sentinel stripped (never leaks; other branches use a sentinel-free recombined body). Indentation: new `Set-CarriedBlockIndent` repairs the first line (Strip's .Trim() zeroes only line 1), dedents by the common prefix, re-indents to the anchor - preserving inner relative indentation; here-string interiors untouched. NB real templates use MULTI-LINE reboot/dialogs `if(){`...`}` blocks (single-line `if(){...}` was only a test artifact). Test-Build 605/0 (+6 FJ#1b/#2b split asserts).

**#1 SIMPLE-LOGIC refinement (r56, user directive):** the code BETWEEN the reboot-check and user-dialogs template blocks is carried VERBATIM (only the whole-script v3->v4 convert touches it) and is NEVER counted as a predecessor uninstall block - EVEN IF IT MATCHES THE FORMAT. Fix (Build.ps1 Build-PredecessorScript PreInstallCode branch): split `$body` at the `#__PB_DIALOGS_SPLIT__` sentinel; run `Split-ExistingUninstallBlocks` + the "Uninstallation of predecessor package" comment strip + `Remove-SelfUninstallGuard` ONLY on the REST (below the dialogs); `$between` is left completely untouched; re-attach the sentinel (Split .Trim()s) then `$body = $between + $rest`. So a between-region block that looks like an uninstall chain stays in place; a below-dialogs one is still extracted/preserved as before. Test-Build 607/0 (+2 FJ#1c asserts: BETWEEN kept, BELOW extracted). Verified end-to-end (Strip-Boilerplate->Set-SectionBody): above between reboot&dialogs, below stays below, no duplicate reboot, parses.

**#1 ROOT-CAUSE FIX (r57, 2026-08-04) - GPF keeps predecessor pre-install AS-IS; conflict was the uninstall-previous logic.** Repro predecessor: `Downloads\SecondtestCases\AES-1-020607-A GNS_Animator4...\Predecessor\GNS_Animator4_x64_2.8.1-0001_en-US` (a **v3** `Deploy-Application.ps1`) with a 2.7.4 uninstall block BOTH between reboot&dialogs AND below dialogs. Bug: the GPF uninstall-previous logic EXTRACTED the below-dialogs block, DROPPED it (immediate-only), and inserted a REGENERATED block nested between reboot&dialogs (via Insert-IntoPreInstall fallback), + the `#__PB_DIALOGS_SPLIT__` sentinel leaked from the predecessor Pre-Uninstall section into the generated block. Fixes (Build.ps1): (1) `Format-OutputScript` strips any leaked split marker (safety net, FJ#1d). (2) For **GPF reuse** (`$isGpfReuse`), the PreInstallCode branch does NO extraction (skips Split-ExistingUninstallBlocks) - body carried verbatim; and the uninstall-previous section adds NO generated block and does NOT drop anything - it ONLY bumps pred-of-pred version -> immediate predecessor (2.7.4 -> 2.8.1). MTB path unchanged (else branch). (3) Insert-IntoPreInstall (MTB fallback) now brace-matches the dialogs block and inserts AFTER it (below), never between. Verified via a real-build harness (`scratchpad/build_pred*.ps1`): between-block stays between, below-block stays below, 2.7.4->2.8.1 (0 vs 13 refs), no sentinel, parses. Test-Build 608/0. Build with a v3 predecessor => reads via `#*==== BEGIN/END ====` marker set (Show-InstallationWelcome), converted to v4 by the whole-script convert.

**#1 REVERTED over-reach (r58, 2026-08-04) - restored the ORIGINAL working uninstall-previous logic; between-code stays a SEPARATE concern.** r57 wrongly gated GPF so it NEVER added the generated uninstall-previous block -> broke the "add predecessor uninstall" checkbox (a predecessor with NO uninstall logic, e.g. Siemens_TmxDIKAB 2408.1100->2408.1700, produced no new uninstall section). Fix: the uninstall-previous section is back to the exact original (`gpfImmediateOnly` drops predecessor-of-predecessor preserved blocks; `AddUninstallPrevious` adds the generated immediate-predecessor block; Insert-IntoPreInstall places the group; pred-of-pred bump only when unchecked). The ONLY separate addition is the between-code isolation in the PreInstallCode branch: split at `#__PB_DIALOGS_SPLIT__`, run Split-ExistingUninstallBlocks/self-guard removal ONLY on the REST (below dialogs), leave the between-code untouched. Insert-IntoPreInstall now brace-matches the dialogs block and inserts the generated group BELOW it (matches the original pre-r49 position; r49 had drifted it to between). Format-OutputScript strips any leaked split marker. Verified with a real-build harness on BOTH: GNS (has uninstall) -> 2.7.4 between-block stays between + generated 2.8.1 block below; Siemens (no uninstall) -> generated block added below. Test-Build 608/0.

**#9 DONE (already handled) + asserted (r49):** reuse already carries the predecessor's ProcToClose into `$adtSession.AppProcessesToClose` (the authoritative GPF field) via the session-field loop in Build-PredecessorScript (`Set-SessionValue -Field 'AppProcessesToClose'`, Build.ps1 ~2181); for Audi/INA it's then moved to NonUI by #16. Added assert FJ#9. No code change needed.

**#3 DONE + deployed (r50, 2026-08-03) — `Show-MsiPropertiesDialog` fully rebuilt (GUI.ps1, XAML-based to dodge PS5.1 Setter-enum bugs):**
- Three new INotifyPropertyChanged classes: `MsiPropRow` (Property/BaseValue/PredValue/YourValue/Apply + computed IsPredDiff[amber]/IsYourEdit[teal]/Change + quiet setters SetValueQuiet/SetApplyQuiet/SetEditedQuiet for init/reset/undo without flagging UserEdited), `MsiCheckRow` (now editable: Property + CheckedValue + Apply), `MstOtherRow` (Replicate/CanApply/Category/Action/Count/Detail/Idx).
- **Master switch** "Carry forward predecessor MST changes" (default ON iff a predecessor MST loaded). ON pre-ticks+amber-highlights pred-diff prop rows and pre-ticks CanApply Other-table removals; OFF reverts all pred-driven (non-hand-edited) rows to base + unticks extras. Base MSI never edited.
- **Tab 1 Properties:** Apply | Property | Base MSI (dim) | Predecessor MST (dim) | Your value (editable) | Change. Amber row = straight from predecessor; teal = your edit. Filter box. "Add property" button.
- **Tab 2 Installer checkboxes:** now EDITABLE (edit CheckedValue + Apply pushes it into the MST output; property rows win on key collision).
- **Tab 3 Other tables (predecessor MST):** Read-MstSettings `OtherItems` shown per-row — Replicate checkbox enabled only for CanApply (Registry/Shortcut/RemoveFile/LaunchCondition/Feature/Environment removals); report-only adds/changes visible but not applied.
- **Undo (Ctrl+Z + button) + Reset:** whole-state snapshot stack; Reset = "Reset to base MSI" (discard all MST changes) so a prior Apply+close is revertible on reopen. Master-toggle undo fixed (CheckBox events fire AFTER the toggle, so the handler overwrites the pushed snapshot's Master with the PRE-toggle value).
- **Change summary** panel: live plain-language list of exactly what the MST will do (+ standard ALLUSERS/REBOOT note).
- Returns `@{ Text=<KEY=VALUE lines>; Extras=@(selected OtherItems); Master=<bool> }`. Callers updated: `BtnMsiPropsView` passes `-PredecessorItems @($State.MstOtherItems) -PredecessorLoaded`, consumes `.Text` + sets `State.MstApplyExtras=@($res.Extras)`; per-command editor consumes `.Text`. New state `MstOtherItems` populated in `Set-PredecessorMstAuto` + the BtnMsiPropsView lazy-load.
- **Dormant test seam** in the function (`if ($script:MsiDlgSmoke)` — unset in prod) bypasses the un-runnable modal loop and returns via the shared `$ui.BuildResult`; headless STA smoke harness at scratch `newdlg.ps1`+`smoke.ps1` (21/21: master on/off, reset, undo incl. master-restore, extras gating, existing-value-wins, new pred props, no-pred). GUI.ps1 parses + dialog XAML loads + Test-Build 592/0. STILL VERIFY IN THE RUNNING APP (WPF not fully auto-testable). Old `Show-MstPlanDialog`/`MstPlanRow` left in place but now unused.

**#3 r51 (2026-08-03) - in-app feedback fixes:** (1) DataGrid column headers were invisible (no header style on the dark theme) -> added a `Hdr` style targeting `pr:DataGridColumnHeader` (root `xmlns:pr` namespace) wired via the Grid style's ColumnHeaderStyle. (2) Added **Delete selected** button (multi-row) beside Add property, undoable. (3) Clarified the value model in labels: columns renamed "Current MSI (ref)" / "Predecessor MST (ref)" / "Your value -> MST"; hint spells out that ONLY Your value is written, Current/Predecessor cols are read-only reference, Your value starts = predecessor when carried (edit to override, untick to skip); Change getter -> "overrides current MSI (was 'x')" / "NEW (not in current MSI)"; summary tags -> "[carried from predecessor MST]" / "[your edit]". (4) Checkbox tab note: installer checkboxes are from the CURRENT MSI only (predecessor never mixed). Smoke 24/24 (adds del + delundo). User chose to make Other-tables freely editable -> done in **r52**.

**#3 r52 (2026-08-03) - Other-tables tab now fully editable (user chose add/delete/edit):** `MstOtherRow` gained editable `Table` (combo of the 6 removable tables: Registry/Shortcut/RemoveFile/LaunchCondition/Feature/Environment) + `Keys` (comma/newline) + `FromPred` + derived `PkCol`; dropped Count/Idx. Grid columns: Replicate | Table (combo) | Key(s) (editable) | Action | What/note. **Add removal** + **Delete selected** buttons on the tab. Amber guardrail label: only REMOVALS transform safely (delete-by-PK, no-op if absent); add/change rows stay CanApply=false/review-only and the build ignores them. `BuildResult` now REBUILDS Extras from the (edited) grid rows via `$ui.PkFor` (table->PK map) - only ticked removals with a known table + >=1 key; no master gate (tick state already reflects master; master on/off toggles only FromPred rows' Replicate, leaving user-added removals). Undo/Restore extended to snapshot otherRows membership + Table/Keys (so Add/Delete/edit are undoable). Headless smoke 17/17 (adds otheradd=2 extras, otheredit applies edited keys, otherdel=0). Test-Build 592/0.

User also changed the GPF template (extra code at top + a picture in the module) - says it works; not verified.
See [[gpf-brand-variant]], [[predecessor-reuse-report]], [[verify-semantic-not-syntax]].
